import PagePlaceholder from '../components/PagePlaceholder';

/** Placeholder so the nav link resolves; KAN-10 fills this in. */
export default function Budgets() {
  return (
    <PagePlaceholder
      title="Budgets"
      description="Set monthly limits per category and track them here."
    />
  );
}
